{
  "name": "draw-and-escape",
  "version": "1.0.0",
  "description": "A creative drawing platformer game where you draw your own path to escape",
  "main": "index.html",
  "scripts": {
    "start": "npx live-server --port=3000",
    "dev": "npx live-server --port=3000 --watch=src/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["javascript", "game", "canvas", "html5", "drawing", "platformer", "puzzle"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}